// TODO(Phase 3, eKYC feature): flesh out the upload-step DTOs (ID front/back,
// selfie), verification result (masked National ID, confidence score,
// attempts remaining, lock state) per SRS FR1 and the UI prompt's eKYC wizard.
export {};
