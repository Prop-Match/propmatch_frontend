// TODO(Phase 3, smart matching feature): flesh out tenant intake (hybrid
// structured fields + open RAG text, Section 3A(D)) and MatchResult/MatchScore
// DTOs, plus the quota-exhausted 403 payload shape (SRS FR2.5).
export {};
